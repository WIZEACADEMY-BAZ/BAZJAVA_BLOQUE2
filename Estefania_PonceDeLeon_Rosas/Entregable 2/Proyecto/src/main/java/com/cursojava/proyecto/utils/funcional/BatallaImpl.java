package com.cursojava.proyecto.utils.funcional;

import com.cursojava.proyecto.model.PokemonDTO;
import com.cursojava.proyecto.utils.exceptions.ExcepcionGenerica;
import com.cursojava.proyecto.utils.threads.CreatePokemon;

import java.util.Random;
import java.util.logging.Logger;

public class BatallaImpl implements Batalla{

    private static final Logger LOGGER = Logger.getLogger(BatallaImpl.class.getName());
    @Override
    public Object simular(Object o, Object o2) {
        //Code no sense
        return o;
    }
}
